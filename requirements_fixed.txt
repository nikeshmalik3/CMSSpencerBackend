# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
aiohttp==3.9.1
pydantic==2.5.2

# Database drivers
motor==3.3.2  # MongoDB async driver
redis[hiredis]==5.0.1  # Redis with C parser for performance
pymongo==4.6.1

# FAISS dependencies (with compatible numpy)
numpy<2.0  # FAISS requires numpy 1.x
# faiss-cpu==1.7.4  # Uncomment when ready to use FAISS

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utils
python-dotenv==1.0.0
pyyaml==6.0.1

# Monitoring
prometheus-client==0.19.0

# Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2