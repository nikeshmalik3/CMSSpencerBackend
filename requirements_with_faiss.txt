# Core dependencies (REQUIRED)
fastapi==0.104.1
uvicorn[standard]==0.24.0
aiohttp==3.9.1
pydantic==2.5.2

# Database drivers (REQUIRED)
motor==3.3.2
redis[hiredis]==5.0.1
pymongo==4.6.1

# Authentication & Security (REQUIRED)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utils (REQUIRED)
python-dotenv==1.0.0
pyyaml==6.0.1

# Monitoring (OPTIONAL)
prometheus-client==0.19.0

# FAISS & Embeddings (OPTIONAL - Install if you want semantic search)
# To enable FAISS, uncomment these lines:
# numpy==1.24.4  # FAISS requires numpy<2.0
# faiss-cpu==1.7.4
# sentence-transformers==2.2.2

# Testing (OPTIONAL)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2